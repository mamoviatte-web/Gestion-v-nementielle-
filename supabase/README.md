# supabase

Scripts SQL à exécuter dans Supabase → SQL Editor, dans l'ordre :

1. `schema.sql` — les 10 tables, contraintes, index, triggers.
2. `rls_policies.sql` — politiques Row Level Security (ROLE_STADE / ROLE_RESPONSABLE).
3. `seed.sql` — 16 espaces, 25 produits, compte admin de test, événement de démo.
